<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ktdsuniversity.edu.members.dao.MemberDao">
    
    <select id="selectMemberByEmail" resultType="com.ktdsuniversity.edu.members.vo.MemberVO"
    parameterType="string">
        SELECT EMAIL
		     , NAME
		     , PASSWORD
		     , SALT
		     , REGIST_DATE
		     , MODIFY_DATE
		     , LATEST_PASSWORD_CHANGE_DATE
		     , LOGIN_DATE
		     , LATEST_LOGIN_IP
		     , LOGIN_FAIL_COUNT
		     , LATEST_LOGIN_FAIL_DAE
		     , BLOCK_YN
		  FROM TMDB.MEMBERS
		 WHERE EMAIL = #{_parameter}
    </select>
    
    <insert id="createJoinMember" parameterType="com.ktdsuniversity.edu.members.dao.MemberDao">
            INSERT INTO MEMBERS
            (EMAIL
            , NAME
            , PASSWORD
            , SALT
            , REGIST_DATE
            , LATEST_PASSWORD_CHANGE_DATE
            , LOGIN_FAIL_COUNT)
            VALUES
            ( #{email}
            , #{name}
            , #{password}
            , #{salt}
            , SYSDATE
            , SYSDATE
            , 0)
    </insert>
    
    
</mapper>